## @(#) : atsushifx's TechBlog

# The URL the site will be built for
base_url = "https://atsushifx.github.io/techbloh/"
title =" atsushifxのてくろぐ"
description = "ふふ…小紅が見守るブログです♡"
default_language = "ja"

# blog theme
theme = "abridge"

taxonomies = [
  { name = "categories" },
  { name = "tags" }
]

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

[markdown]
highlight_code = true

[extra]
recent_posts_limit = 5

[extra.authors]
atsushifx = { name = "atsushifx", role = "執筆", icon = "🖊" }
kobeni    = { name = "小紅", role = "見守り担当♡", icon = "🩷" }
tsumugi   = { name = "つむぎ", role = "サポート＆ツッコミ", icon = "🎀" }
